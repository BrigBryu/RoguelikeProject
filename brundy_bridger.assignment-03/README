This dungeon generator creates a procedurally generated 80x21 dungeon conforming to the project specifications. It ensures:
- Generation
    - A minimum of 6 rooms connected, with sizes at least 4x3.
    - Rooms are separated by at least one cell of non-room space.
    - At least one up and one down staircase, ensuring all floors are connected.

- File Save/Load:
    - Saves dungeon state to `~/.rlg327/dungeon` in big-endian format.

- Path Finding
    - Creates two path finding algorithms one considering tunoling and one not conisdering it.
    - For the tunoling it will have a U for unmuntablek

Key functions:
- dungeonDijkstraNonTunnel(), DungeonDijkstraTunnel(), generateDungeon(), renderDungeon(), readDungeon() and loadDungeon()
- File names: dungeon.h, dungeon.c, rectangle.h, point.h, fileHandler.h. fileHandler.c, heap.h, heap.c

